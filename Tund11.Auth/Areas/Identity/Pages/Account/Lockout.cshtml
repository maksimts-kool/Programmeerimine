@page
@model LockoutModel
@{
    ViewData["Title"] = Localizer["Identity_LockedOutTitle"];
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@Localizer["Identity_LockedOutMessage"]</p>
</header>