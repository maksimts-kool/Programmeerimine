@model Ankeet

@{
    ViewData["Title"] = "Tänan!";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="alert alert-success" role="alert">
                <h1>Tänan!</h1>
                <hr />
                <p class="lead">Suur aitäh vastamise eest ankeetile <strong>@Model.Nimi</strong>!</p>
                <p>Sinu vastus on edukalt salvestatud.</p>

                @if (Model.OnOsaleb)
                {
                    <div class="alert alert-info mt-3" role="alert">
                        <h5>🎉 Õnnitleme!</h5>
                        <p>Väga tore, et tuled osalema! Näeme sind üritusel!</p>
                    </div>
                }
                else
                {
                    <div class="alert alert-warning mt-3" role="alert">
                        <h5>😢 Kahjuks kuuled!</h5>
                        <p>Kuid me oleme kurvad, et sa kahjuks tulla ei saa. Võib-olla järgmise korra!</p>
                    </div>
                }

                <div class="card mt-4">
                    <div class="card-body">
                        <h5 class="card-title">Ankeedi detailid</h5>
                        <dl class="row">
                            <dt class="col-sm-4">Üritus:</dt>
                            <dd class="col-sm-8">@Model.Pyha?.Nimetus</dd>

                            <dt class="col-sm-4">Ankeedi nimi:</dt>
                            <dd class="col-sm-8">@Model.Nimi</dd>

                            <dt class="col-sm-4">Sinu vastus:</dt>
                            <dd class="col-sm-8">@(Model.OnOsaleb == true ? "Jah, tulen kindlasti" : "Ei, kahjuks ei saa                       tulla")</dd>

                            <dt class="col-sm-4">E-post:</dt>
                            <dd class="col-sm-8">@Model.Epost</dd>
                        </dl>
                    </div>
                </div>

                <div class="mt-4">
                    <a class="btn btn-primary" asp-area="" asp-controller="Home" asp-action="Index">Tagasi
                        kodulehele</a>
                </div>
            </div>
        </div>
    </div>
</div>